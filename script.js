document.addEventListener('DOMContentLoaded',()=>{const nav=document.querySelector('.nav');addEventListener('scroll',()=>{nav?.classList.toggle('is-scrolled',scrollY>20);document.documentElement.style.setProperty('--scroll',Math.min(scrollY/(document.body.scrollHeight-innerHeight),1))},{passive:true});const items=document.querySelectorAll('.project,.post,.intro-grid,.statement,.contact-card,.feature-story,.archive-card,.journal-heading,.journal-end');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12});items.forEach(e=>{e.classList.add('reveal');io.observe(e)});document.querySelectorAll('.visual,.feature-art,.archive-visual').forEach(v=>{v.addEventListener('pointermove',e=>{const r=v.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;v.style.setProperty('--mx',`${x*10}px`);v.style.setProperty('--my',`${y*10}px`)});v.addEventListener('pointerleave',()=>{v.style.setProperty('--mx','0px');v.style.setProperty('--my','0px')})});document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;document.querySelectorAll('.archive-card').forEach(card=>card.classList.toggle('hidden',filter!=='all'&&card.dataset.category!==filter))}));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth',block:'center'})}}))});