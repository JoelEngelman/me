const nav=document.getElementById('siteNav'),line=document.getElementById('scrollLine'),cursor=document.getElementById('cursor'),label=document.getElementById('cursorLabel'),menu=document.getElementById('menu'),mobile=document.getElementById('mobileMenu');
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
function scrollUI(){const h=document.documentElement.scrollHeight-innerHeight,p=h>0?scrollY/h:0;if(line)line.style.width=(p*100)+'%';if(nav)nav.classList.toggle('scrolled',scrollY>30)}
addEventListener('scroll',scrollUI,{passive:true});scrollUI();
if(menu)menu.addEventListener('click',()=>mobile.classList.toggle('open'));if(mobile)mobile.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobile.classList.remove('open')));
const reveals=document.querySelectorAll('.reveal');if(reduce)reveals.forEach(x=>x.classList.add('visible'));else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.08,rootMargin:'0px 0px -8% 0px'});reveals.forEach((e,i)=>{e.style.transitionDelay=Math.min(i%5*.07,.28)+'s';io.observe(e)})}
if(cursor&&!reduce&&matchMedia('(pointer:fine)').matches){let cx=innerWidth/2,cy=innerHeight/2,tx=cx,ty=cy;addEventListener('mousemove',e=>{tx=e.clientX;ty=e.clientY},{passive:true});function move(){cx+=(tx-cx)*.18;cy+=(ty-cy)*.18;cursor.style.left=cx+'px';cursor.style.top=cy+'px';if(label){label.style.left=cx+'px';label.style.top=cy+'px'}requestAnimationFrame(move)}move();document.querySelectorAll('[data-cursor]').forEach(el=>{el.addEventListener('mouseenter',()=>{cursor.classList.add('active');if(label){label.textContent=el.dataset.cursor;label.classList.add('show')}});el.addEventListener('mouseleave',()=>{cursor.classList.remove('active');if(label)label.classList.remove('show')})})}
if(!reduce&&matchMedia('(pointer:fine)').matches){document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.16,y=(e.clientY-r.top-r.height/2)*.16;el.style.transform=`translate(${x}px,${y}px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});document.querySelectorAll('.magnetic-card').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(1000px) rotateX(${y*-2.2}deg) rotateY(${x*2.2}deg)`});el.addEventListener('mouseleave',()=>el.style.transform='')})}
document.querySelectorAll('a[href$=".html"]').forEach(a=>a.addEventListener('click',e=>{const href=a.getAttribute('href');if(href&&href!=='#'&&!reduce){e.preventDefault();document.body.style.transition='opacity .22s';document.body.style.opacity='.35';setTimeout(()=>location.href=href,180)}}));